{# ===========================================================
Template: homepage/index.twig
URL: /
===========================================================
Homepage template
=========================================================== #}

{% extends '_layouts/_base' %}

{% import '_macros/02_components/_assets' as assets %}
{% import '_macros/02_components/_sitemap' as sitemap %}
{% import '_macros/03_modules/_opener' as openers %}

{% block content %}
  {% set homepage = craft.entries().section('homepage').one() %}
  {% set opener = homepage.openerHomepage %}
  {% set builder = homepage.builderHome %}

  {# Opener #}
  {{ openers.home(opener) }}

  {# Builder #}
  {% if builder %}
    {% include '_partials/_builder-homepage.twig' with {'builder': builder} %}
  {% endif %}

  {# Sitemap #}
  {{ sitemap.sitemap() }}
{% endblock %}